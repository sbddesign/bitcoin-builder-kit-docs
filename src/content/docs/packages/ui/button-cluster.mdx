---
title: Button Cluster
description: An arrangement of buttons.
---

import  { Tabs, TabItem } from '@astrojs/starlight/components';

## Example

<div class="bui-component-group" style="margin: 2rem 0; padding: 1rem; border: 1px solid var(--system-divider); border-radius: var(--size-2); background: var(--background); display: flex; flex-direction: row; gap: 8px;" data-theme="conduit" data-mode="dark">
    <bui-button-cluster direction="horizontal">
        <bui-button style-type="outline" label="Back" cluster="left"></bui-button>
        <bui-button style-type="outline" label="Save" cluster="middle-horizontal"></bui-button>
        <bui-button style-type="outline" label="Next" cluster="right"></bui-button>
    </bui-button-cluster>
</div>

### Import

<Tabs>
    <TabItem label="JavaScript">
    ```javascript
    import '@sbddesign/bui-ui/button.js'
    import '@sbddesign/bui-ui/button-cluster.js'
    ```
    </TabItem>
     <TabItem label="React">
    ```jsx
    import { BuiButtonReact as BuiButton, BuiButtonClusterReact as BuiButtonCluster } from '@sbddesign/bui-ui/react'
    ```
    </TabItem>
</Tabs>

### Usage

<Tabs>
    <TabItem label="JavaScript">
    ```html
    <bui-button-cluster direction="horizontal">
        <bui-button style-type="outline" label="Back" cluster="left"></bui-button>
        <bui-button style-type="outline" label="Save" cluster="middle-horizontal"></bui-button>
        <bui-button style-type="outline" label="Next" cluster="right"></bui-button>
    </bui-button-cluster>
    ```
    </TabItem>
     <TabItem label="React">
    ```jsx
    <BuiButtonCluster direction="horizontal">
        <BuiButton styleType="outline" label="Back" cluster="left"></BuiButton>
        <BuiButton styleType="outline" label="Save" cluster="middle-horizontal"></BuiButton>
        <BuiButton styleType="outline" label="Next" cluster="right"></BuiButton>
    </BuiButtonCluster>
    ```
    </TabItem>
</Tabs>

## Properties

The direction property is neither required nor reflected.

| Property | Attribute | Type | Description |
|----------|-----------|------|-------------|
| `direction` | `direction` | string | No | No | Layout direction: "horizontal", "vertical" |

### Valid Values

#### Directions
- `horizontal` - Buttons arranged in a row
- `vertical` - Buttons arranged in a column

### Behavior

- **Layout Management**: The button cluster automatically manages the layout of child buttons
- **Spacing**: Provides consistent spacing between buttons in the cluster
- **Direction Control**: The `direction` property controls whether buttons are arranged horizontally or vertically

### Events

The button cluster component does not emit custom events. It acts as a container for buttons and manages their layout and spacing.
